[build]
builder = "nixpacks"

[deploy]
# Use workspace-aware start command so Prisma and API run in apps/api context
startCommand = "pnpm --filter @lighter-bot/api start:railway"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
