{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @lighter-bot/common build && pnpm --filter @lighter-bot/dashboard build",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "pnpm install",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- apps/dashboard/ packages/common/"
}
